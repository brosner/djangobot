#!/bin/bash
DOMAIN="botland.oebfare.com"
PROJECT="djangobot_project"
ROOT="${HOME}/webapps/${DOMAIN}"
PROJECT_ROOT="${ROOT}/${PROJECT}"
SERVER_ROOT="${ROOT}/oebland"
VENV_ROOT="${HOME}/virtualenvs/${DOMAIN}"